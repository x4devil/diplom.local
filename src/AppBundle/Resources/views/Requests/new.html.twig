{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Requests creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('requests') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
