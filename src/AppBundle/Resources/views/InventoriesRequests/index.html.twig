{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InventoriesRequests list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Date</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('inventoriesrequests_show', { 'id': entity.id }) }}">{{ entity.date }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('inventoriesrequests_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('inventoriesrequests_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('inventoriesrequests_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
