{% extends '::baseAdmin.html.twig' %}

{% block body -%}
    <h1>Inventories creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('inventories') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
