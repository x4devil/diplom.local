{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InventoriesProperties list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Value</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('inventoriesproperties_show', { 'id': entity.id }) }}">{{ entity.value }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('inventoriesproperties_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('inventoriesproperties_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('inventoriesproperties_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
